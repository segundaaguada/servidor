import styled from 'styled-components';

const Input = styled.input`

    &.admin-checkbox {
        height: 20px;
        width: 20px;
        position: absolute;
        top: 20px;
        right: 20px;
        cursor: pointer;
    }

`

export default Input;